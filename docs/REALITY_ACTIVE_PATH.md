# Reality Active Path

The Reality layer is now available as a deterministic contract and compiler. Before the next production image, the active concept/prompt path must call `reality_preflight()` and consume `compile_reality_prompt()` or equivalent compiled constraints.

Until that integration is verified, a generation must be treated as a benchmark rather than a production asset.
