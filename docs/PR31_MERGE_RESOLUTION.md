# PR #31 Merge Resolution

The ZeroGPU branch diverged from `main` because `main` advanced with the Kaggle worker controller while the ZeroGPU branch advanced independently.

The branch now preserves both Adobe technical commands and Kaggle worker controls, and includes the Kaggle worker controller from `main`.

This is an audit note for the merge-resolution step.